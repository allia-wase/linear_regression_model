# Render Blueprint - API at Summative/API
services:
  - type: web
    name: depression-prediction-api
    env: python
    buildCommand: pip install -r Summative/API/requirements.txt
    startCommand: cd Summative/API && uvicorn main:app --host 0.0.0.0 --port $PORT
